# Интерактивные механики

Откройте [`index.html`](index.html) в браузере (двойной клик или `python3 -m http.server` из корня репо).

Без npm. Без ассетов Apple/Stripe. Цель — пощупать пайплайн, sticky, canvas-scrub, язык кейфреймов и vertex-displacement.
