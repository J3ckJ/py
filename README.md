# Python: не учебник, а карта

Репозиторий `J3ckJ/py` был пустым. Здесь две карты: **Python** (язык и крупные кодовые базы) и **презентационный веб** (как делают сайты уровня Apple/Stripe — пайплайн браузера, скролл-таймлайн, видео/WebGL). Не энциклопедия интернета, а разбор по первичным источникам и живому HTML.

Снимок: **август 2026**. Стабильный CPython — **3.14.7**. Python **3.15.0rc1** уже вышел (финал запланирован на 1 октября 2026). Примеры в `examples/` прогоняются на том Python, который есть в окружении (сейчас это 3.12); конструкции 3.14/3.15 описаны отдельно и помечены версией.

## Чем это не является

- Не курс «с нуля за выходные».
- Не каталог всех библиотек PyPI.
- Не копии чужих репозиториев и не клон apple.com (авторское право: разбор, короткие цитаты, свои демо).
- Не обещание, что после чтения вы «знаете Python». После чтения вы будете **знать, куда смотреть**, когда код врёт.

## Как устроено

Читать по порядку в первый раз. Потом — прыгать по проектам.

| Папка | Зачем |
| --- | --- |
| [00-как-читать.md](00-как-читать.md) | Метод: как разбирать любой Python-код |
| [01-язык/](01-язык/) | Модель языка: объекты, имена, протоколы, MRO, импорт |
| [02-runtime/](02-runtime/) | CPython: байткод, GIL / free-threading, память, типы |
| [03-ремесло/](03-ремесло/) | stdlib, concurrency, упаковка 2026, как читать чужой код |
| [04-проекты/](04-проекты/) | Анатомия живых кодовых баз |
| [05-система/](05-система/) | От скрипта к системе: паттерны, которые повторяются везде |
| [06-сайты/](06-сайты/) | Как устроены сложные сайты: Apple, Stripe, Linear, анимации |
| [sites/](sites/) | Интерактивные HTML-демо (sticky, canvas-scrub, WebGL cloth) |
| [examples/](examples/) | Маленькие программы, которые *показывают* модель, а не синтаксис |
| [SOURCES.md](SOURCES.md) | Первичные источники со ссылками |

### Проекты, разобранные по коду

Исходники читались из GitHub (shallow clone, август 2026):

- **requests** + **httpx** — как устроен HTTP-клиент
- **Flask** + **Werkzeug** — WSGI, контексты, роутинг
- **FastAPI** + **Starlette** — ASGI, DI, Pydantic как компилятор контракта
- **Pydantic v2** — Python строит схему, Rust валидирует
- **pytest** — pluggy, коллекция, фикстуры, assertion rewriting
- **Django** — `WSGIHandler` → middleware onion → `QuerySet`
- **SQLAlchemy ORM** — `Session` как Unit of Work

CPython разобран по Language Reference, What's New 3.14/3.15 и документации free-threading, не по полному дереву `Python/ceval.c` (это отдельная книга).

## Одна мысль, которая держит всё остальное

В Python **нет переменных в смысле C**. Есть объекты и имена, которые на них ссылаются. Весь «магический» код крупных библиотек — это эксплуатация нескольких протоколов: дескрипторы, контекст-менеджеры, итераторы, вызываемые объекты, метаклассы, import hooks. Когда вы видите `request.args` в Flask или `Depends(...)` в FastAPI, вы видите не синтаксис языка, а объекты, которые реализуют эти протоколы.

Если читать одну главу — начните с [01-язык/02-имена-объекты-привязки.md](01-язык/02-имена-объекты-привязки.md), затем сразу [04-проекты/00-метод.md](04-проекты/00-метод.md).

## Запуск примеров

Python:

```bash
python3 examples/run_all.py
```

Сайты (открыть в браузере):

```bash
python3 -m http.server 8000
# http://127.0.0.1:8000/sites/
```

Если читать одну главу про веб — [06-сайты/07-apple.md](06-сайты/07-apple.md) и сразу [sites/index.html](sites/index.html).
