# Примеры

Маленькие программы к главам `01-язык/` и `02-runtime/`. Не учебные «hello world», а проверки модели.

```bash
python3 examples/run_all.py
```

Каждый файл можно запускать отдельно. Если assert падает — сломана не «программа», а ваше понимание (или регрессия интерпретатора).
